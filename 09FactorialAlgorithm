An Algorithm which gives the result of factorial function:
n! = n * (n — 1) * (n — 2) * ... * 2 * 1

Input:
N = Number
i = 1;


Process:

if N > 1:
step 1:  N = i * i++
Step 2: For as long as i++ = N, i++ 
