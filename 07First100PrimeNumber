Algorithm that print the first 100 Prime number:

N = Number
D = Divisor
R = Remaindor
P = Prime Number

Input:
N = Input;
D = 2

Process:

If N > 1;
  N / D:
If N % D == 0;
  break;
else 
  N / D++
  
